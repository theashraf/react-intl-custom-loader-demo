/* eslint-disable */

class IntlPlugin {
    apply(compiler) {
        /*
            get all .lang files
            and concat them into a single object
            create a virtual module
            add it as a dependency to the entry module
        */
        console.log('hello')
    }
}

module.exports = IntlPlugin